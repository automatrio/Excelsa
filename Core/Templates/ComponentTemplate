using Excelsa.Core;
using Excelsa.Core.Base;
using Excelsa.Extensions;

namespace Excelsa.Domain.Components
{
    class #COMPONENT_NAME#Component : ComponentBase
    {
        private static readonly string path = "";

        protected override string ScrollIntoViewScript { get; set; } = $"document.querySelector('{path}').scrollIntoViewIfNeeded()";
        protected override string ScrollIntoViewFallbackScript { get; set; } = $"document.querySelector('{path}').scrollIntoViewIfNeeded()";

        public #COMPONENT_NAME#Component(
            SpecificationBase specification)
            : base (
            "Validating the component #COMPONENT_NAME#",
            "The component #COMPONENT_NAME# could not be validated.",
            specification
            )
        { }

        public override void MainFlow()
        {
            
        }
    }
}
